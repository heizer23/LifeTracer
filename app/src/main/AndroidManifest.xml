<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyTheme"
        tools:targetApi="31">
        <activity
            android:name=".views.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".views.VaultActivity"
            android:label="Instance Vault"
            >
        </activity>

        <activity
            android:name=".views.DetailActivity"
            android:label="Instance Details"
            >
        </activity>

        <activity
            android:name=".views.ReviewActivity"
            android:label="Instance Details"
            >
        </activity>

        <activity
            android:name=".views.TestActivity"
            android:label="Instance Details"
            >
        </activity>

        <activity android:name=".views.ActivitySubTask" />
    </application>

</manifest>